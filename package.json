{
  "scripts": {
    "build:css": "grunt build:css",
    "build": "npm run build:css --prefix themes/hyde-y && hugo",
    "test": "echo \"Error: no test specified\" && exit 1",
    "watch": "grunt watch"
  },
  "devDependencies": {
    "grunt": "^1.0.4"
  }
}
