{
  "main": "Gruntfile.js",
  "devDependencies": {
    "grunt": "^1.3.0",
    "grunt-cli": "^1.3.2",
    "grunt-contrib-cssmin": "^3.0.0",
    "grunt-contrib-less": "^1.4.1",
    "grunt-contrib-watch": "^1.1.0"
  },
  "scripts": {
    "build:css": "grunt build:css --prefix themes/hyde-y",
    "build": "npm run build:css --prefix themes/hyde-y && hugo",
    "test": "echo \"Error: no test specified\" && exit 1",
    "watch": "grunt watch"
  }
}
